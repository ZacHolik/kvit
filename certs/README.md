# FINA CIS TLS lanac

Ovdje stavi puni PEM lanac za CIS test (npr. `fina_full_chain.pem`) kako bi Node
povjerovao FINA DEMO CA. Datoteka se ne commita (vidi `.gitignore`).

Zadani put u kodu: `certs/fina_full_chain.pem` (ako nisu postavljeni
`FINA_CA_CERT_PATH` / `FINA_CA_CERT_BASE64`).
