# Novi vodič / alat — checklista

1. Dodati metadata sa SVIM OG poljima (`buildVodicMetadata` / `buildAlatMetadata` iz `@/lib/og-metadata`)
2. `PageTopBar` ispod H1
3. `BottomCTA` na dnu
4. (Vodiči samo) Razmotriti `InlineCTA` ako je vodič dug i tema odgovara (`po-sd`, `razred`, `doprinosi`, `interni-akt`)
5. OG slika 1200×630 u `public/og/` ili koristiti `/opengraph-image` fallback
